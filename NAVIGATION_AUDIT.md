# Аудит навигации - Найденные проблемы

## ❌ ПРОБЛЕМА 1: Использование context.push<T>() для получения результата
**Где:** practice_screen.dart (строки 13, 27)
**Код:**
```dart
final result = await context.push<Practice>('/practice/create');
if (result != null && context.mounted) {
  context.read<PracticeListCubit>().addPractice(result);
}
```
**Почему это проблема:**
- Смешивает навигацию с бизнес-логикой
- Затрудняет тестирование
- При возврате с экрана результат теряется
- push должен быть для навигации, не для обмена данными

**Правильное решение:** Использовать Cubit для управления состоянием, не ждать результат из push

---

## ❌ ПРОБЛЕМА 2: Navigator.of().pop() вместо context.pop()
**Где:** 
- practice_screen.dart (строка 50)
- practice_detail_screen.dart (строка 341)
- practice_playback_screen.dart (строки 30, 290)

**Почему это проблема:**
- Смешивание go_router и Navigator API
- Непредсказуемое поведение в GoRouter контексте
- Может нарушить stack навигации

**Правильное решение:** Везде использовать context.pop() или context.go()

---

## ❌ ПРОБЛЕМА 3: context.go с string interpolation вместо named routes
**Где:** practice_screen.dart (строка 93), practice_detail_screen.dart (строка 250)
**Код:**
```dart
context.go('/practice/${practice.id}');
context.push('/practice/${practice.id}/playback');
```

**Почему это проблема:**
- Невозможно отследить все маршруты через codebase
- Хрупкий код - легко ошибиться в пути
- Невозможно использовать refactor-tools
- Не используется named routes из GoRouter конфига

---

## ❌ ПРОБЛЕМА 4: context.push для edit практики вместо context.go
**Где:** practice_detail_screen.dart (строка 53)
**Код:**
```dart
final result = await context.push<Practice>('/practice/create', extra: practice);
```

**Почему это проблема:**
- CreatePracticeScreen должен быть modal/overlay, а не push в stack
- После редактирования нужно вернуться на detail, а не на list
- Смешивание push и go логики

---

## ❌ ПРОБЛЕМА 5: Использование context.pop() для возврата с бизнес-логикой
**Где:** practice_detail_screen.dart (строка 341-342)
**Код:**
```dart
context.read<PracticeListCubit>().deletePractice(practice.id);
Navigator.of(context).pop();
context.go('/practice');
```

**Почему это проблема:**
- pop() может быть обработан раньше go()
- Непредсказуемое поведение
- Две операции навигации подряд

---

## ✅ ИСПРАВЛЕННАЯ СХЕМА МАРШРУТОВ

```
/practice (list)
  ├── /practice/create (overlay/modal)
  ├── /practice/:id (detail)
  │   └── /practice/:id/playback (full screen)
  │       └── /practice/:id/playback/summary (после завершения)
/statistics
/me
```

**Правила:**
- `/practice` - list view (context.go для переключения между tabs)
- `/practice/create` - modal (context.push для создания нового)
- `/practice/:id` - detail view (context.push из list)
- `/practice/:id/playback` - full-screen playback (context.push из detail)

---

## 📋ЧЕК-ЛИСТ ИСПРАВЛЕНИЙ

- [ ] Заменить context.push<T>() на Cubit для управления данными
- [ ] Заменить Navigator.of().pop() на context.pop()
- [ ] Использовать named routes вместо string interpolation
- [ ] Разделить modal и push навигацию
- [ ] Убрать ожидание результата из push
- [ ] Объединить операции навигации в одну
